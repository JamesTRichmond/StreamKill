name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  repo-sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repo sanity
        run: |
          test -f README.md
