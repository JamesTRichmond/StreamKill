# StreamKill

Project bootstrap setup.

## Commit Convention (for automated releases)

Use Conventional Commits:

- `feat: add X` -> minor release
- `fix: correct Y` -> patch release
- `feat!: change API` or `BREAKING CHANGE:` in body -> major release
- `docs: ...`, `chore: ...` -> no release (unless configured)

