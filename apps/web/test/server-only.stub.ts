// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component bundle. Vitest aliases `server-only` here so the
// server-scoped trust modules (contract, engine, store) can be unit-tested.
export {};
